/* Paste the code below in your browser's console and run it to fill in most
 * of the options in a survey page to speed up testing.
 * Sliders must be manually entered.
 * You may still have to manually enter one input to be able to continue.
 */

$("select").prop('selectedIndex', 1); // select first option for each dropdown
$("div > input").click(); // select all radio buttons
$("input[type='text']").val(42); // fill all text boxes with "42"
